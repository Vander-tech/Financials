// Custom JS code can go here, such as form validation or interactive features
document.querySelector('form').addEventListener('submit', function(event) {
    event.preventDefault();
    alert('Form submitted!');
  });